import * as AppTypes from "./AppTypes/AppTypes";

/**
 * Import AppTypes file (./AppTypes.ts) which contains every 'namespace'
 * in /AppTypes directory (./AppTypes) and export it here.
 *
 * This creates namespaces with ES2015 import/exports.
 */

export { AppTypes };
